---
description: Work with OpenSRE — the open-source AI SRE agent framework (install, run investigations, integrations, deploy, develop)
argument-hint: "[what you want to do, e.g. 'onboard with Datadog' or 'run an investigation']"
---

Use the **opensre** skill to help with the following OpenSRE task: $ARGUMENTS

If no task is given, briefly summarize what OpenSRE is and offer the common entry
points (install, `opensre onboard`, run an investigation, set up an integration,
develop from source).
