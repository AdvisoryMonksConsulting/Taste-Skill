// Live demo of the landing-page template.
// The canonical, reusable kit lives in /templates/landing-page — this route just
// mounts it so the page is buildable, prerendered, and shareable as a spec demo.
// To create a real client demo: copy /templates/landing-page into a new route folder
// (e.g. src/app/demos/<client>/) and edit its content.ts.
export { default, metadata } from "../../../../templates/landing-page/page";
